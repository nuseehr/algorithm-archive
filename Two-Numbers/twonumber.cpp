#include <iostream>
#include <string>
#include <vector>
#include <map>

using namespace std;

bool solution(vector<int> nums, int target) {
    
    for(auto e : nums) cout << e << endl;
}


int main() {
    vector<int> nums;
    int target = 9;
    solution(nums, target);
}
