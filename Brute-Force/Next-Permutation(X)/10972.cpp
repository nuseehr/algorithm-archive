#include <isotream>

using namespace std;

int arr[10001];

int main() {
    int N;
    cin >> N;
    for (int i=0; i<N; i++) cin >> arr[i];
    
    for (int i=N-1; i>0; i--) {
        if (arr[i-1] < arr[i] ) {
        }
    }
    
    
}
